@model Autoservis.KlijentInfoList

@{
    ViewBag.Title = "Klijenti";
}

<table class="table table-striped table-dark">
    <tr>
        <th>
            Prezime
        </th>
        <th>
            Ime
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
            /* {
                 <tr>
                     <td>
                         @Html.ActionLink(item.NazOsobe, "Details", new { IdKlijenta = item.IdKlijenta })
                     </td>
                     <td>
                         @using (Html.BeginForm("Delete", "Klijent", new { IdKlijenta = item.IdKlijenta }))
                         {
                             <a class="delete">Obriši</a>
                         }
                     </td>
                 </tr>
             }*/
            {
                <tr>
                    <td>
                        @Html.Encode(item.Prezime)
                    </td>
                    <td>
                        @Html.Encode(item.Ime)
                    </td>
                    <td class="text-right">
                        @Html.ActionLink("Detalji", "Details", new { IdKlijenta = item.IdKlijenta },new { @class="btn-sm btn-primary",style="text-decoration:none;"})
                        @using (Html.BeginForm("Delete", "Klijent", new { IdKlijenta = item.IdKlijenta },FormMethod.Post,new { style="display:inline;"}))
                        {
                            <a class="delete btn-sm btn-danger">Obriši</a>
                        }
                    </td>
                </tr>
            }

</table>

<p>
    @Html.ActionLink("Novi klijent", "Create")
</p>